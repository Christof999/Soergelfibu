rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Nutzer können nur Dateien in ihrem eigenen Ordner lesen/schreiben
    match /projects/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
